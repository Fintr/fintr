Let's change our gameplan. Instead of creating actual data, which will rely on the performance of the internet, let's just show what the user can do instead. So in both the mobile and the desktop view, can you just show around the buttons and say what they can do? 

1. Mobile, Click on the `+` button
2. Show the `+ Add Transaction` button.
2.1. Desktop, show the `+ Add Transaction` button for it.
3. Click on it, show the `Expense` tab.
4. Show the `Income` tab. Explain what you can do here.
5. Click on the `Income` tab to show the form.
6. Highlight the `Deduct Taxes` checkbox pill, explain to use it if you want to reduce taxes that should take effect monthly for the whole year.
7. Highlight the `Deduct Contributions` if they want to reduce also the government contributions of their income.
8. Highlight the `Transfer` tab. Explain that this is what you'll use to `Transfer cash from one account to another`.
9. Highlight the `Loan` tab. Explain that this is what you use to `register loans whether you're the borrower or the lender of the loan`. Show that they should try adding a `percentage` to see how much they'll lose or earn from the loan schedule.
10. Desktop, highlight to the `Loan` tab from the dashboard.
10.1. Mobile, highlight the `Menu` from the dashboard.
10.2. Mobile, click on the `Menu` from the dashboard.
10.3. Mobile, highlight the `Loan` tile / block on the menu.
10.4. Desktop and Mobile, Explain that you click on the `Loan` highlighted to view the loans created. They can see the loan schedule and how much they can earn here.
11. Mobile, Click on the `+` button. We will show the `Add Receipt` functionality
11.1. Mobile and Desktop, Click on the `Add Receipt`, 
11.2. Mobile and Desktop, Highlight `Take Photo` explain to use it for taking photos of the receipts.
11.3. Mobile and Desktop, Highlight `Upload File` explain to use it to upload files of the receipts.
11.4. Mobile and Desktop, Highlight the whole `Add Receipt` modal that holds the `Take Photo` and `Upload File`. Explain that this one helps in identifying inputs for your receipt like amount, date, account, category.
12. End the session.
 