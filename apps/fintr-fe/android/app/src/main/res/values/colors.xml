<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Top bar / safe-area background (bg-background, dark theme) -->
    <color name="fintr_top_bar">#151921</color>

    <!-- Bottom navigation background (bg-primary) -->
    <color name="fintr_bottom_nav">#0A3D62</color>

    <!-- Theme colors (required by styles.xml references) -->
    <color name="colorPrimary">@color/fintr_bottom_nav</color>
    <color name="colorPrimaryDark">@color/fintr_bottom_nav</color>
    <color name="colorAccent">@color/fintr_bottom_nav</color>
</resources>
