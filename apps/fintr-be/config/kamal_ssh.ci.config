# GitHub Actions only — set KAMAL_SSH_CONFIG_PATHS=config/kamal_ssh.ci.config in CI.
# Ephemeral runners have no persistent known_hosts; skip host key verification.

Host api.fintr.ai fintr.ai www.fintr.ai 34.21.206.91
  StrictHostKeyChecking no
  UserKnownHostsFile /dev/null
