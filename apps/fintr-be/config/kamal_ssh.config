# Referenced from config/deploy.yml (ssh.config). Kamal cwd should be apps/fintr-be (see bin/kamal).
# Tighter limits than OpenSSH defaults so dead connections fail faster instead of sitting silent ~10+ minutes.

Host *
  ConnectTimeout 30
  ServerAliveInterval 15
  ServerAliveCountMax 4
